#include<iostream>
using namespace std;
#include<climits>
int main(){
    int n;
    cin>>n;

    int a[n];
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    int maxOfI=INT_MIN;
    for(int i=0;i<n;i++){
        maxOfI=max(maxOfI,a[i]);
        cout<<maxOfI<<endl;
    }
    cout<<"max of i:"<<maxOfI<<endl;
    return 0;
}
